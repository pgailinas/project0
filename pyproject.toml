[project]
name = "project0"
version = "0.1.0"
description = "Documentation-first AI software development platform"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}

authors = [
    {name = "Phil Gailinas"}
]

dependencies = [
    "mkdocs",
    "mkdocs-material",
    "pymdown-extensions",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

